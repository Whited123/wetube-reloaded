extends ../base
include ../mixins/video

block content
    div.profile__data
    if loggedInUser.socialOnly
        img(src=user.avatarUrl).avatar
    else
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 동영상이 없습니다.