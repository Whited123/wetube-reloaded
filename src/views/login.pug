extends base.pug

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="username", type="text", required, placeholder="Username")
        input(name="password", type="password", required, placeholder="Password")
        input(type="submit", value="Login")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=Iv1.cc536f8660f504cf&allow_signup=false") 깃허브에서 계속하기 &rarr;
    hr
    div 
        span 계정이 없습니까? 
        a(href="/join") 회원가입 하러가기 &rarr;       